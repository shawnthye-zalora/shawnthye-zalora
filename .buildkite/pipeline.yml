steps:
  - label: ':console:'
    command:
      - "echo 'Hello'"
    artifact_paths:
      - 'README.md'
      - "app/build/outputs/bundle/**/*.aab"
      # - "app/build/outputs/bundle/debug/app-debug.aab"
    plugins:
      - docker-compose#v3.10.0:
          run: fastlane
